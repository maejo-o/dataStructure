/*
#include <stdio.h>
int main(void)
{
    int i, length = 0;
    char str[50];
    printf("Input the string : ");
    scanf("%s", &str);
    printf("inputed string is ... \n");
    for(i=0; i < 50; i++)
    {
        printf("%c", str[i]);
        length += 1;
    }
    printf("\nThe string's length is ... \n");
    printf("%d", length);
    return 0;
}
*/
